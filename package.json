{
  "name": "acme-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "1.0.0-beta.127"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs --port=5000",
    "build": "redocly bundle -o dist/bundle.yaml",
    "test": "redocly lint"
  }
}
